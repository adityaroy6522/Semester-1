#include <stdio.h>
int main ()
{
    int n,num;
    printf ("how many number do you want to enter? ");
    scanf ("%d",&n);
    for (int i=1;i<=n;i++)
    {
        printf ("enter number %d ",i);
        scanf ("%d",&num);

        if (num>0)
        {
            
        }
    }
  
    return 0;
}