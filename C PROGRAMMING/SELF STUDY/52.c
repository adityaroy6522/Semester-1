#include <stdio.h>
int main ()
{
    int x=10, y=20;
    if (x==y);                        //if if statement has semi columb ; than printf becomes independent
    printf ("\n%d %d",x,y);
    return 0;
}